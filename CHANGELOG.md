# Changelog

本项目遵循语义化版本。日期使用 Asia/Shanghai。

## [Unreleased]

## [0.6.11] - 2026-09-14

### Added

- **全岛对比度审计**（`npm run audit:contrast`，`scripts/audit-contrast-ui.mjs`）：在隔离 Electron 实例里逐分区、逐主题量**真实渲染像素**的 WCAG 对比度。每个文字元素的矩形内取"出现最多的量化色"当背景、与背景亮度差最大的像素当文字，因此渐变、半透明层、`backdrop-filter` 都被如实计入——按 CSS 颜色栈推断背景会在玻璃面板上给出"背景与文字同色 → 1.0:1"这种物理上不可能的结论。覆盖 `::placeholder`（不在 `textContent` 里，纯 DOM 扫描必然漏掉）、滚动区外元素（用命中测试排除，否则会量到旁边内容的像素）、emoji（多色字形，WCAG 不适用，排除）。硬失败线 3.0，3.0–4.5 之间作为"有意弱化层级"列出供复核。

- **待办优先级安全取值**：`logic/todo.ts` 新增 `prioOf()`。原实现 `PRIO[t.priority]` 在任何越界/历史/被手改的值（如 `"high"`、`4`）上得到 `undefined`，读 `.ring` 立即抛错——**渲染期异常会让 React 卸载整棵树，整个岛变成空白**，不只是坏掉那一条待办。

### Fixed

- **展开态岛面板顶角的割裂感**（用户反馈"展开时还是存在"）。根因是**结构性的**：顶部的两个外扩凹弧是两个独立的 div，它们拿不到面板本体的氛围渐变与背面模糊，只能填一块平色，因此在凹角处必然有色差。改为**单一元素**：面板盒子两侧各留 21px，外轮廓用 `clip-path: path()` 一次画完（凹弧圆心落在 `(R,0)`，与主体侧边在 `y=R` 处相切，接合点是数学相切、天然平滑），内容用等量内衬抵消，视觉布局与原来完全一致；轮廓、背景、`backdrop-filter` 都只有一份，接缝在结构上不存在。面板高度经 `ResizeObserver` 跟随（各分区内容长短不一）。像素验证：凹弧带的水平相邻色差与面板内部同一水平（深色主题 23–32），不再有台阶。

- **浅色主题下"发出去的消息气泡"文字几乎不可见**。气泡底色是填充式强调色（`accent(0.52)→accent(0.44)`），浅色主题下 `--accent1-l-shift` 为负，实际落到 l≈0.24 的深绿，而文字用的是随主题翻转的 `ink(1)`（浅色下近黑）→ 实测约 1.2:1。改用 `solidInk()`（恒为亮墨）：**深色主题零变化**（两者是同一条公式、只差 `--ink-l` 与 `--solid-ink-l` 两个变量，深色下取值相同），浅色主题升到约 11:1。

- **输入框占位提示在浅色主题下读不清**。原先没有 `::placeholder` 规则，落到浏览器默认的 `darkgray`（约 54% 黑），实测 1.9–2.6:1。改用 `ink(3)` 一档，与"次级提示"同层且随主题翻转。

- **语义色在浅色主题下当文字不可读**。`sem.warn/danger/focus/calm/run` 是跨主题固定的硬编码值（明度 0.7–0.8，为深色面板选的），浅色面板上实测只有 **1.1–1.2:1**（"今日 +3 XP"、"88"、"展开"这类全糊在底上）。明度经新增的 `--sem-text-max-l` 钳制（浅色 0.40、深色 1），因此前景、描边、浅底填充一次性受益；深色主题该变量为 1，取值与历史完全一致。终端预览、便签放映层、终端拖拽遮罩这三处恒为深底的局部表面显式重置为 1。

- **墨色阶梯整体上调**（两主题）。全岛像素实测显示深色主题的 `ink(3)`（`text.faint()`，全应用用得最多的一档）只有 2.70–2.99:1、`ink(4)` 只有 1.8–2.3:1（时间戳、"52 字"、"填入主输入框"、会话标题都属这两档），浅色主题的 `ink(4)` 在有色底上也只有 1.75–2.5:1。深色 ink3/ink4 由 0.32/0.24 上调到 0.50/0.40，浅色由 0.62/0.44 上调到 0.66/0.52。这同时修掉了 v0.6.10 记录的"已知问题：深色 ink3 实测 2.70:1"。

- **以强调色为前景色的残留写法**（约 20 处）。上一版迁移了 131 处 `color: accent(...)`，但漏掉了三元表达式（`color: x ? accent(0.85) : ink(2)`）、设计系统自身的 `Chip` 活动态与 `Button` 的 `tinted` 变体、以及"文字叠在彩色环/彩色格上"的几处（洞察盘刻度、周趋势数字）。浅色主题下这些实测 1.8–2.9:1，现全部走 `accentText()`／改用与底色匹配的墨色。

- **待办分组标题的计数被二次衰减**：`opacity: 0.65` 叠在 `ink(3)` 上，有效 alpha 只剩约 0.33（实测 2.37–2.58:1）。墨色层级本身已表达主次，去掉多余的透明度。

- **分析方法论在本机 Agent 引擎下不可用**。选 Claude Code / Codex 作为问答引擎时，"回答分析中心"的方法论仍走云端模型守卫（`!apiKey` 直接返回），表现为**全部执行失败**。现在本机引擎不依赖云端 Key，分析同样交给它执行，并复用既有流式管线（思考/步骤/正文）。

- **分析失败不再毫无痕迹**。原实现失败只弹 4.2s toast 且随即关闭面板，而"回答分析中心"仅在 `analyses.length > 0` 时才渲染，用户会以为"什么都没发生"。现在失败会写进气泡（`⚠ 方法名失败：原因`）。

- **本地免密钥端点（Ollama / LM Studio）整链路可用**。`llm-proxy` 里 3 处 `if (!cfg.apiKey) return { ok: false, error: 'API Key 未配置' }` 硬校验与渲染层遗留的 `!apiKey` 判定叠加，使免密钥端点实际不可用。主进程不再硬性要求 Key（云端缺 Key 时上游 401 已有可读提示），渲染层统一到 `llmUsable()`（14 处），本地端点只要选了模型即可用。

### Verification

- 41 个离线测试脚本、两套 TypeScript 检查、生产构建。
- `npm run audit:contrast`：2 主题 × 11 分区、共 1372 个带文字元素的像素样本，硬失败（<3.0）**由 146 条降到 0 条**；3.0–4.5 的 89 条为有意的弱化层级（时间戳、微型元数据等），逐条列出供复核。
- `npm run audit:ask`、`npm run audit:terminal`：隔离 Electron 下的交互与布局审计（面板改为单一元素轮廓后重新验证）。
- 顶角轮廓的像素复核：凹弧带的水平相邻色差与面板内部同一水平，接合处无台阶。

## [0.6.10] - 2026-09-14

### Fixed

- 浅色主题文字可读性（用户反馈"文字读起来吃力"）。根因是**套用的是一套为深色主题校准的透明度阶梯**：深色文字叠在浅色面板上会更细更虚，同一 alpha 在浅色下天然更弱。实测渲染像素的对比度变化：

  | 区域 | 修复前 | 修复后 |
  | --- | --- | --- |
  | 次级说明（`text.dim` → ink2） | 5.09 | 6.88 |
  | 时间戳（`text.mono` + ink3） | 2.87 | 4.48 |
  | 筛选标签行（ink2） | 4.40 | 5.87 |
  | 强调色分组小标题（`accent()` 作文字） | 2.40 | 4.51 |

  三处改动：
  1. **墨色阶梯**：浅色主题的 ink2/3/4 透明度由 0.66/0.46/0.30 上调到 0.78/0.62/0.44。其中 ink3 最关键——`text.faint()` 映射到它，是全应用用得最多的一档文字（221 处调用 + 148 处直接引用），原先 2.8:1 连大字号 AA（3.0）都没到。
  2. **强调色文字**：`accent()` 的明度参数是按深色主题设计的（深色下 `accent-l-shift ≈ 0`，所以传 0.85 就是亮色），浅色下同一 shift 为负，默认值落到 0.54、传 0.85 落到 0.57，作为文字只有 2.4–2.9:1。新增 `accentText()`：同签名，但浅色主题把明度钳到 0.40。131 处 `color: accent(...)` 全部迁移；终端预览、便签放映层、终端拖拽遮罩这三处恒为深底的局部表面显式重置 `--accent-text-max-l: 1`，避免在深底上被压暗。**深色主题下 `accentText()` 与 `accent()` 计算值完全相同**（实测两者都解析为 `oklch(0.85 0.14 168 / 0.9)`），零回归。
  3. **最弱提示档 ink4**：原值在两个主题下都低于任何可读底线（浅色 1.87 / 深色 1.54），现为浅色 0.44 / 深色 0.24（深色 1.54 → 1.91），仍明显弱于 ink3，层级不变。

### 已知问题

- 深色主题的 ink3（`text.faint()`，时间戳等次级内容）实测 2.70 : 1，与浅色修复前（2.87）相当。深色下亮字叠暗底有光晕，观感好于数值，且调整会明显改变现有深色观感，故本版未改；如需提升，把 `--ink-3-a` 由 0.32 调到 0.42 可达 3.70 : 1。

### Verification

- 两套 TypeScript 检查、41 个离线测试、Electron 三端生产构建通过。
- 全部对比度数字取自隔离 Electron 实例的**真实渲染像素**：CDP 截图 → 自写 PNG 解码 → 分带计算"字芯亮度 vs 面板底色"的 WCAG 对比度，深浅两套主题分别测量。

## [0.6.9] - 2026-09-14

### Added

- 自动化引擎事件化：定时自动化新增四种事件触发器——「Agent 会话结束」「番茄钟专注结束」「会议开始前 N 分钟」「会议结束后」，与原有「每日 HH:mm」并存。触发器在设置页可选（会前可填 1–120 分钟），动作仍复用现有三种（运行工作流 / 写待办 / 存便签）。会话与专注按状态边沿判定、会议按时间窗口判定，都不在时间轮询里重复触发；会议去重键随规则持久化，应用重启不会对同一场会议重复触发。旧版仅含 `time` 的规则在水合时自动迁移为每日定时触发器，磁盘数据随首次保存升级。

### Changed

- 迷你条贴顶圆角改为单一元素轮廓：此前「主体 + 两侧角补块」是两个独立元素，两个表面拼接导致接缝反复出现（色差、背景模糊不一致、接合区被描边切成内部亮线）。现改为主体盒子两侧各留 18px，用 `clip-path: path()` 按实际宽度精确剪出「圆角矩形 + 两侧外扩凹角」的完整剪影——背景渐变、背景模糊、投影同属一个元素，没有第二个表面可对不齐；凹弧圆心位于主体边缘外 18px，与主体侧边在 y=18 处**相切**，接合点是几何相切而非调参凑平。同时移除了原本 1px 的轮廓描边（`--line-l` 18%，在浅色主题下是一条突兀的暗线，实测贴边像素 146,161,176 对主体 180,198,214），改由玻璃表面的色差、投影与底部内高光定义边界。
- 代码块复制按钮改为悬停/键盘聚焦时浮现：原先每个代码块都常驻一个「⧉ 复制」，一条含多段代码的回答会同时出现多个复制按钮，且与气泡级、菜单级「复制」范围不同却不作区分，既吵闹又容易误点。现默认隐藏、悬停或 Tab 聚焦时浮现（`div` 换成真实 `<button>` 才能被键盘聚焦，否则 `:focus-within` 形同虚设），按钮收敛为纯图标并带 `title`，代码块右侧预留内边距从 46px 收到 30px；无悬停能力的触屏设备保持常显。
- 录屏预览播放头脱离 React 状态：video 的 `timeupdate` 是高频事件，此前每次触发都会让录屏工作台（2000+ 行组件）整树重渲染。现改为写 ref + 直接改 DOM（`style.left` / `textContent`），React 因其它原因重渲染时从 ref 读最新值保持一致——沿用终端字符回显的「零 React 状态更新」惯例。

### Fixed

- 迷你条内边距补偿：两侧外扩区占用的 18px 需计入所有横向内边距，原先只有基础规则加了补偿，`compact` 变体的 `padding: 7px` 会把状态图标推到轮廓之外切成两半。现统一由 `--notch` 变量参与计算。
- 问答分支 id 改为严格单调序列：原实现 `now*100 + random(0..99)` 在同一毫秒内新建两个分支有约 1% 概率撞 id，撞上时消息会同时写入两条会话（静默串线）。已加入 2000 次连续创建不重复的回归测试。
- 审批等待计时表按当前待审批集合修剪，长会话下不再无限累积 requestId。
- 终端关闭标签页时清理该会话的执行状态与待执行命令队列（会话 id 不复用，原为缓慢泄漏）。
- 录屏预览的 `withTimeout` 在先完成一侧回收定时器，不再遗留悬挂定时器。

### 已核实但不修改

- 资讯日报修剪与便签热力图所用的日期键均为同函数内自洽使用（且 `feedDailies` 键已零填充），字典序与时间顺序一致，审查清单中「跨月删错」一项不成立。
- 全局快捷键 effect 未加依赖数组（每次渲染重挂监听）：功能正确且闭包始终最新；改写需把 40 行按键处理迁移到 ref 方案，为微秒级开销引入破坏 Esc/Y/N 语义的风险，故有意保留。

### Verification

- 两套 TypeScript 检查、41 个离线测试、Electron 三端生产构建通过。
- 迷你条轮廓改动以像素扫描在深浅两套主题下实测：接合区无内部亮线、外扩量与几何吻合、贴边像素与主体一致（Δ1），并在真实隔离实例中确认 `compact` 变体下状态图标完整。
- 自动化触发器与旧数据迁移在真实实例中验证：会议/番茄触发器持久化往返正常，旧版规则迁移后 `time` 值保留。

## [0.6.8] - 2026-09-14

### Added

- 审批策略规则化：设置页可配置放行规则（子串匹配命令原文），命中即自动放行并记入审计流水，把注意力留给真正危险的命令；审批卡片新增「本会话放行此命令」，同一会话内相同命令不再重复询问，会话结束自动清理。策略由主进程在阻塞审批前强制执行，启动时即从持久化载入。
- 定时自动化：快捷页新增「定时自动化」编辑器，可配置每日 HH:mm 触发一条动作——运行指定工作流、写入待办或存为便签；应用启动时自动补跑 30 分钟内错过的任务（标注「补跑」），同日不重复触发。
- 本地模型预设：供应商目录新增 Ollama 与 LM Studio（回环地址 + OpenAI 兼容 `/v1`，无需 API Key）。选中即视为已就绪，可用模型通过「同步可用模型」自动发现；本地推理数据不出网。配套放开了运行状态、保存、发送与 RAG 等 7 处密钥守卫，空密钥不再发送空 `Bearer` 头。
- 新增审批策略、定时自动化与附件外置离线测试，离线测试 38 → 41 个。

### Changed

- 运行时升级到 Electron 44.3.0（原 39.8.10 已滑出官方安全维护窗口）。Electron 44 重构了剪贴板 API（`read`/`write` 走 ClipboardItem、`readText`/`writeText` 改为 Promise、移除 `readImage`/`writeImage`），新增 `src/main/clipboard-compat.ts` 兼容层保持调用方语义不变；截图轮询改为异步串行并加入重入保护。
- 图片附件外置：问答消息里的截图 base64（thumb + dataUrl，单张数百 KB）不再随状态全量加密落盘，改为按内容哈希存入 `userData/attachments/` 并在读取时回填。实测含 3 张截图的 `config.json` 从 2.3 MB 降到 0.6 KB；渲染层内存模型不变（仍持有 dataUrl），重复图片自动去重，写入失败保留内联形态不丢图，未被引用的附件在 7 天宽限后自动清理。
- 测试运行器改为管道转发子进程输出并对失败重试一次：规避本机 Node 25.2.1 (Windows) strip-types 退出阶段的偶发 libuv 断言崩溃（断言均通过、仅影响进程退出；CI 使用 Node 22 不受影响）。

### Verification

- 两套 TypeScript 检查、41 个离线测试、Electron 三端生产构建通过。
- Electron 44 升级验证：NSIS 打包、隔离安装/启动/卸载（`verify:package`）、真实运行时启动、剪贴板写入读回往返、应用内真实 ConPTY 会话与界面渲染截图。
- 附件外置验证：真实应用内 `save → reload → load` 往返（dataUrl 逐字节一致）、`config.json` 体积与附件目录实测。

## [0.6.7] - 2026-09-13

### Added

- 自动更新：接入 electron-updater + GitHub Releases。启动 30 秒后静默检查、之后每 6 小时轮询，新版本后台下载，用户确认后重启安装（退出时兜底安装）；设置页「运行状态」新增检查更新与重启安装入口。更新链路从本版本起闭环。
- 问答会话导出：分支面板新增「导出」，把当前分支全文（含长期上下文标记与文本附件）写入 Markdown 文件，归档会话以附录列出。
- Agent 卡片补齐 Claude 元信息：Stop 事件从会话 transcript 尾部提取模型名与上下文占用（输入+缓存），岛卡片显示模型徽章，终端面板的 Token/上下文指标与占用条对 Claude 会话点亮（此前仅 Codex 可用）。
- 主题设计器新增五组「复古风·高级感」风格起点：奶霜、月苍、牙色、御茶、肌色——奶油底 + 双强调色映射为 OKLCH 令牌，主色明度已校准至 WCAG AA。
- 新增 push/PR 日常 CI（typecheck + 离线测试 + 生产构建，windows-latest）；新增 RSS 解析与知识库纯逻辑离线测试，离线测试 36 → 38 个。

### Fixed

- 便签 AI 回写改为按 id 补丁式更新：AI 请求期间的并发修改（星标/置顶/锁定/编辑保存）不再被过期整对象覆盖导致静默丢数据。
- 安全加固：`agent-cli-check` 引擎白名单（堵命令注入）；录屏工程删除先查注册表（堵路径穿越）；`save-md-file` 只允许回写本会话经对话框打开/保存过的路径；Markdown 图片走协议白名单并惰性加载（远程信标不再随渲染发出）；快捷指令 shell 步骤引用 `%prev%`/`%clip%`/`%input%` 时即使「信任」也强制确认（防 AI 输出被注入命令静默执行）；`github-readme` 校验 owner/repo；外部抓取（RSS/网页）8MB、知识库 PDF/DOCX 30MB 响应体上限；导出 PDF 离屏窗口补齐导航锁。
- hook-installer：对 `~/.claude/settings.json` 等用户全局配置改为 tmp+rename 原子写，内容无变化跳过写入（不再每次启动重写）；Codex `notify` 键插入首个 TOML 表段之前（原追加到文件末尾会静默失效）；Codex 审批 hook 补 600s 长超时。
- 审批并发队列：同一会话并行工具调用产生多个挂起审批时排队展示、依次裁决，不再互相覆盖导致 CLI 悬挂至 5 分钟超时；会话结束时排队审批全部 fail-open 交回 CLI。
- 性能：save-state 主进程 700ms 防抖并在退出时强制落盘（流式回答期间不再高频全量写盘）；Markdown 组件 memo 化；媒体轮询内容未变不再触发全树重渲染；codex-tail 会话头部只读前 16KB（长 rollout 不再全量载入内存）。
- 稳定性：统一 `safeSend` 销毁守卫（托盘/快捷键/推送通道）；本地桥启动失败降级继续初始化（不再产生无窗无托盘僵尸进程）；positionWindow 过期的 60ms 重试按代数作废；终端会话退出后自动重启保留 profile/工作目录/环境变量与尺寸；录屏 finalize/discard 与分片写入串行化（不再产生孤儿 .part 文件）；会议检测轮询 10 秒超时强杀续链；三处诊断日志超 1MB 轮转。
- 打包：仅被 vite 打包消费的依赖移到 devDependencies、移除未使用的 electron-store，app.asar 从 261MB 降至 33MB，安装体积大幅缩小。
- 测试：修复 terminal-workspace 测试的时间腐化（快照日期钉死导致发版一周后必然失败）。

### Verification

- 两套 TypeScript 检查、38 个离线测试、Electron 三端生产构建通过。
- NSIS 打包验证 `latest.yml`/`.blockmap` 生成；unpacked 以隔离 profile/bridge/hooks 冒烟启动通过，electron-updater 随包就位。

## [0.6.6] - 2026-07-23

### Added

- 问答输入区新增“本轮回答方法”：15 种方法按表达组织、推理求解、决策判断、创新设计和行动落地分类，并根据问题关键词在本地推荐适合方法。
- 单轮方法指令统一接入云模型、本地 Claude/Codex、知识库 RAG 和快捷问题发送路径；发送后自动恢复默认，用户问题气泡保留方法标记。
- 回答气泡分析中心扩展到 23 种方法，覆盖事实证据、逻辑因果、风险压力、决策取舍和执行验证；同类分析覆盖更新，不进入后续主会话上下文。
- 新增隔离 Electron 问答审计 `npm run audit:ask`，覆盖方法选择、分析中心、气泡菜单、未处理异常和窄宽响应式布局。

### Changed

- 回答气泡“更多”菜单按上下文、继续使用、沉淀与复用重新分组，并新增“引用整条回答”；气泡内继续追问仍保留在原回答下。
- 回答方法与分析方法卡片改为自适应网格，窄面板自动切换单列，避免功能密集时挤压文字。

### Verification

- 两套 TypeScript 检查、36 个离线测试、Electron 三端生产构建和隔离问答 UI 审计通过。
- NSIS 安装包的 unpacked 启动、静默安装、正式启动与卸载验证通过。

## [0.6.5] - 2026-07-23

### Fixed

- 修复新建第二个 PowerShell 标签后，UTF-8 与退出码提示符初始化命令通过 PTY 注入并与用户输入竞争，导致输入行抖动、命令交错或字符被吞的问题。
- PowerShell 5.1/7 初始化改为进程启动参数，在进入交互提示符前完成；CMD 与 WSL 启动行为保持不变。
- 修复创建或切换第二个终端时，旧的面板自动收起计时器可能先于新 xterm 焦点生效，使整个终端在输入过程中执行 `translateY(-101%)` 向上收起动画的问题。

### Verification

- 隔离 Electron 终端审计新增第二个独立 PowerShell 会话，验证逐字符输入期间终端几何不变、命令正常执行并返回 `exit 0`。
- 两套 TypeScript 检查、35 个离线测试和 Electron 三端生产构建通过。

## [0.6.4] - 2026-07-22

### Added

- 终端开发现场恢复：加密保存标签、工作目录、Shell、历史、项目工作区、启动任务、环境配置和可选输出快照，重启后可选择恢复、自动恢复或新建空白现场。
- 终端效率中心：项目任务扫描、Git 上下文、历史检索与收藏、输出折叠摘要、路径拖入与点击、危险命令行内确认，以及 PowerShell 5.1/7、CMD、WSL 会话配置。
- 终端 AI 诊断、交接摘要和下一步规划；所有建议仅展示，不自动执行命令。
- 终端工作区导入导出、输出快照脱敏/保留策略和环境变量安全存储。

### Fixed

- 修复灵动岛旧的自动收起计时器会在终端获得键盘焦点后继续执行，导致输入时整个终端框向上移动的问题。
- 修复 PowerShell 5.1 不支持 `` `e`` 转义写法，可能把提示符控制序列显示为普通文本的问题。
- 继续保持 xterm fit 逐帧合并、ConPTY 尺寸去重和 PTY 字符回显零 React 状态更新。

### Verification

- 两套 TypeScript 检查、35 个离线测试、Electron 三端生产构建和隔离 Electron 终端审计通过。
- NSIS 安装包的 unpacked 启动、静默安装、正式启动与卸载验证通过。

## [0.6.3] - 2026-07-22

### Changed

- PowerShell 工具栏的文件夹入口改为 Windows 原生目录选择器；确认后对路径进行 PowerShell literal 转义，并立即切换当前 ConPTY 会话的工作目录。
- 目录选择器接入统一 External Yield 原生对话框包装，打开期间降低灵动岛最高层级，关闭后恢复。

### Fixed

- 修复 PTY 每次字符回显都会从旧提示符重复识别相同目录、创建新标签数组并触发终端模块重渲染的问题。
- 修复 `ResizeObserver` 连续通知、xterm resize 事件和显式 IPC resize 叠加造成的输入行抖动与 ConPTY 无效重排。
- 主进程现在记录每个 PTY 的最后尺寸并忽略重复 resize；会话退出或关闭时同步清理尺寸状态。

### Verification

- 新增终端目录状态引用稳定性和原生目录选择器 External Yield 接入回归。
- 两套 TypeScript 检查、33 个离线测试、Electron 三端生产构建、真实 Electron 连续输入几何检查、NSIS 打包及隔离安装/启动/卸载验证通过。

## [0.6.2] - 2026-07-21

### Changed

- OpenAI 官方目录升级到 GPT-5.6 Sol / Terra / Luna；快速与深度模式分别映射低/最大 reasoning effort，并改用 `max_completion_tokens`，不再发送旧 `max_tokens` 或不兼容的 `temperature`。
- Claude 官方目录升级到 Sonnet 5、Opus 4.8、Fable 5 与 Haiku 4.5；按型号分别映射 adaptive thinking、effort、显示摘要和 Haiku 手动思考预算。
- 知识库 Embedding 改为独立保存 Base URL、模型和 API Key；旧版只保存模型的配置会一次性迁移当前连接，之后切换问答供应商不再影响 RAG。
- 设置页和问答模型菜单显示连接来源与配置编号；每条新云端回答记录实际请求模型，后续切换模型仍可追溯回答归属。

### Fixed

- 修复同一供应商、模型和端点下的不同账号配置在保存时互相覆盖，以及设置页未比较密钥却错误标记“使用中”的问题。
- 修复仅存在于已保存配置中的模型不会出现在问答切换菜单的问题；保存与载入现在会原子同步供应商、模型、端点、密钥和型号目录。
- 修复多模型讨论把同模型、同端点但不同账号误判为重复配置，以及当前配置不可用时共识/辩论主持请求失败的问题。
- 修复切换到 Anthropic 或 Kimi Code 聊天模型后，知识库仍复用聊天端点而导致向量检索和会话写入失效的问题。

### Verification

- 新增 GPT-5.6、Claude 5、不同账号配置、独立 Embedding 迁移与请求体兼容回归；气泡内继续追问、上下文隔离和分支回写原有测试保持通过。
- 两套 TypeScript 检查、33 个离线测试、Electron 三端生产构建和生产依赖审计通过。

## [0.6.1] - 2026-07-21

### Changed

- 重构问答交互层级：高频操作直接展示，分支、记忆与多模型讨论归入会话管理；操作入口改为带结果说明的文字动作和执行反馈。
- 保留每条回答气泡内的“继续追问”支线；回答分析、多模型候选和汇总结论固定附着到原回答，不再形成难以辨认的主会话消息。
- 新会话和 Fork 增加执行前说明，消息上下文状态改为常驻可见标记。
- Claude 供应商改为原生 Anthropic Messages 协议，支持 `x-api-key` 鉴权、图片输入、模型目录发现和 adaptive thinking；默认目录迁移到 Sonnet 4.6、Opus 4.8 与 Haiku 4.5。
- DeepSeek V4 快速/深度模式现在真实控制 thinking 与 reasoning effort；Kimi K3 映射 low/max effort，Kimi Code 固定使用三个官方 Model ID。

### Fixed

- 修复追问、回答分析或多模型讨论期间切换分支时，异步结果可能按消息序号串写到另一会话的问题；结果现在按稳定分支 ID 回写发起分支。
- 修复主回答、知识库回答、记忆压缩和分支合并在切换会话后仍可能写入当前分支的问题；同一分支生成期间现在会阻止重复发送，其他分支仍可独立使用。
- 修复本机 Claude/Codex 依赖 CLI 全局最近会话、无法可靠遵守岛内分支上下文的问题；两种引擎现在显式携带岛内历史、记忆和会话规则。
- 修复置顶上下文在主进程请求层被二次截断、历史附件无预算增长、归档会话残留永久执行中状态的问题。
- 修复删除父分支后子分支保留失效谱系，以及本机 Agent 停止后部分输出可能被误标为正常完成的问题。
- 修复知识库检索等待期间切换会话会误用另一分支规则，以及历史截尾会丢失较早“重要内容”的问题；持久化现在在 60 条限额内优先保留重要消息。
- 修复同一供应商内修改模型、地址或密钥时，旧的连通测试/模型同步响应仍覆盖当前配置的问题；同端点不同账号的已保存模型也可在问答区切换。
- 修复误填完整 `/chat/completions` 或 `/messages` 地址、复制配置带首尾空白后请求失败的问题；HTTP 错误体和网络异常统一按当前密钥脱敏。
- 将 Kimi For Coding 会员服务与 Kimi 开放平台拆分为独立供应商，分别使用 `api.kimi.com/coding/v1` 和 `api.moonshot.cn/v1`，修复会员密钥被发送到开放平台后返回 401 的问题。
- 新增 `kimi-for-coding`、`kimi-for-coding-highspeed`、`k3` 官方模型目录，并迁移旧版手动 Kimi Code 配置；旧 Kimi 密钥会一次性预填到新入口，原开放平台配置仍保留。
- Kimi 请求改用 `max_completion_tokens` 并省略受思考模式约束的显式 `temperature`，避免认证通过后继续触发模型参数错误。

### Verification

- 问答回归新增气泡内追问、分析结果附着、同类分析替换、生成锁定、临时状态清理、本机 Agent 上下文、附件预算和主上下文隔离检查；隔离 Electron 实例已重建并人工检查问答真实截图。
- 新增 Kimi 双端点、配置迁移和请求体兼容回归；两套 TypeScript 检查、33 个离线测试和 Electron 三端生产构建通过。
- 新增 Anthropic 原生请求、DeepSeek thinking、Kimi K3 effort、错误脱敏、Base URL 归一化、Claude 模型迁移和重要上下文持久化回归。

## [0.6.0] - 2026-07-21

### Added

- 问答新增会话分支树：可从任意提问或回答 Fork，保留父子谱系、独立命名、快速切换，并可将其他分支压缩后合并进当前长期记忆。
- 新增会话级长期记忆和持续指令；消息可钉选为永久上下文或排除出模型请求，文本附件会在后续轮次持续生效，并提供真实上下文/token 预算检查。
- 新增多模型会诊：从已保存模型中选择 2–4 个并行回答，可提炼共识或主持分歧辩论；候选答案可切换查看并采用为后续主上下文。
- 新增回答自检、假设与风险、替代路径、问题拆解、苏格拉底式澄清、知识库核验和下一问建议。
- 对话全文、单条回答和框选片段可直接写入本地向量知识库，作为新的会话知识源立即参与 RAG 检索。

### Changed

- 问答空态推荐与常驻迷你条改为同源动态内容池，复用启用主题、AI/内置/GitHub/工作简报内容和轮播节奏。
- DeepSeek、Kimi 等供应商改为独立持有模型、Base URL 与 API Key；支持旧配置迁移、在线 `/models` 目录同步和账号实际可用模型切换。

### Fixed

- 修复切换 DeepSeek、Kimi 时串用 API Key、旧持久化模型目录覆盖新默认值，以及上游认证错误回显密钥片段的问题。
- 修复打包后审计/安装验证实例忽略 `AIISLAND_BRIDGE_FILE`、仍可能覆盖真实 bridge discovery 的隔离缺口。

### Verification

- 新增并扩展问答与供应商回归测试，覆盖 Fork 截断、长期记忆、上下文钉选/排除、附件连续上下文、知识库 Markdown 序列化、旧配置迁移、供应商密钥隔离和目录升级。
- 两套 TypeScript 检查、32 个离线测试、Electron 三端生产构建和生产依赖审计通过。
- 8 张隔离 Electron 真实截图已重建；NSIS 安装包、unpacked 启动、静默安装启动、隔离 bridge 与静默卸载验证通过。

## [0.5.1] - 2026-07-20

### Changed

- 录屏定位框改为直接消费每一帧真实源裁剪参数；区域录制、完整显示、铺满画面、横竖屏/方形输出和鼠标运镜不再分别计算坐标。
- 录屏试播增加实时取景小地图；显示器使用真实物理帧尺寸，窗口来源在媒体轨就绪后使用实际 `videoWidth/videoHeight`。
- 录屏渲染健康度改为统计 Canvas 实际绘制节拍，避免暂停恢复后误报源视频掉帧。
- 开发截图、架构说明、录屏管线、测试说明、安装说明和 Release 工作流同步到 `0.5.1`。

### Fixed

- 修复选择 A 显示器但预览短暂绑定 B 显示器、窗口标题被显示器标签覆盖，以及应用自身窗口来源过滤不完整的问题。
- 修复自定义区域与动态运镜定位框不一致、区域内光标光效偏移、奇数源尺寸出现一像素边界误差的问题。
- 修复暂停恢复后时间线倒退、收起控制条导致录制 Canvas 被卸载、结束关键帧丢失或初始关键帧重复的问题。
- 修复窗口录制定位框使用估算尺寸，导致窗口裁剪范围与真实媒体轨不一致的问题。

### Verification

- 动态定位框直接复用实际 `drawImage` 源裁剪参数，区域、完整显示与铺满组合由录屏几何回归测试覆盖。
- 窗口来源媒体轨就绪后使用实际视频尺寸，显示器/窗口选择与源坐标映射回归测试通过。
- 两套 TypeScript 检查、32 个离线测试和 Electron 三端生产构建通过。

## [0.5.0] - 2026-07-20

### Added

- 新增专业录屏工作台：显示器/窗口/区域采集、鼠标聚焦运镜、音频混合、摄像头画中画、人物替换、本地动漫化、录制健康监控和悬浮录制控制条。
- 新增长录制分片落盘、异常恢复、磁盘素材直出、录屏工程库、自动保存、跨重启续编、工程副本和 `v2` 工程文件。
- 新增视频/音频/章节三轨时间线，多片段拆分与修剪、磁吸、缩放、轨道锁定和 40 步撤销/重做。
- 新增真实音频转写、SRT/VTT、AI 结构化粗剪、章节与画面增强建议。
- 新增 MP3 音频导出、独立输出分辨率/帧率，以及 MP4/WebM 可开关字幕轨。

### Changed

- 录屏导出统一走内置 FFmpeg，支持 MP4、WebM、GIF、MP3 和多档压缩；字幕时间码会随保留片段与播放速度重排。
- 录制预览和最终合成共用画面管线，区域裁切、鼠标运镜、人物形象和录制框保持一致。

### Fixed

- 修复导入人物头像重启后失效、录制区域与动效框不一致、录制状态不可感知、停止后无可用预览、窗口来源选择不稳定等问题。
- 修复运行审计可能写入实际用户数据目录的问题；测试实例现在显式使用隔离 `userData` 和 bridge 发现文件。

## [0.3.0] - 2026-07-19

### Added

- 设置页显示器选择使用真实显示器列表（新增 `getDisplays` IPC，展示分辨率/DPI 缩放/主屏标记，替代硬编码双屏假图）。

### Changed

- **视觉体系 Apple（macOS/iOS）化**：层级从"1px 描边制"改为 Apple 填充制——新增 `fill(1-4)` 填充阶梯与 `hairline()` 发型线分隔（0.5px），卡片/容器全面去描边；圆角对齐 Apple 连续圆角阶梯（按钮 10/卡片 13/浮层 18/面板 28）；排版对齐 SF（标题负字距、iOS label 四级墨色、SF 字体栈优先）；分段控件改 iOS 滑动 thumb（layoutId 弹簧）、开关改 iOS 白钮、按钮改圆角矩形（新增 tinted 变体）；按压反馈改 iOS 式透明度下沉；设置/待办/观察清单等列表改 inset grouped 分组（行间 hairline 左缩进分隔，新增 `Group` 组件）。
- **前端视觉全面重设计**（功能零改动）：新建 `src/renderer/src/ui/` 设计系统——`tokens.ts`（层级表面/排版阶梯/间距圆角/语义色令牌，消费 OKLCH 主题变量）、`components.tsx`（Button/IconButton/Card/Chip/Badge/Input/Segmented/SectionHeader/EmptyState/Switch/Slider 共享组件库）、`motion.ts`（framer-motion 动效预设）、`icons.ts`（lucide 语义图标表）。
- 全部 11 个分区 + 12 个浮层/工作室 + 应用外壳（头部/Tab 栏/toast）重做到设计系统：emoji 图标全面替换为 lucide，Tab 栏改 layoutId 滑动胶囊指示器 + 分区图标，浮层统一 `surface.overlay()` + overlayPop 弹出，卡片入场统一 fadeScaleIn（仅 opacity/scale/filter，保留"禁 translate"约束）。
- 材质进阶：面板顶部极光氛围光 + 玻璃颗粒噪点层 + 底部渐变收边高光 + 主题色环境投影；Tab 栏下签名渐变分割线；主按钮 hover 流光；空态图标光晕；审批卡琥珀注意力脉冲；待处理 Tab 声呐环；滚动条悬停才浮现。
- 全屏模式恢复真全屏：窗口从工作区切到整个物理显示器（`display.bounds`，screen-saver 层级盖任务栏），退出回到工作区。
- 新增依赖 framer-motion（动效）；AmbientBar 迷你条材质与设计令牌对齐（动效体系不变）。
- 新增全局样式：文本选区主题色、输入焦点环（.ui-input）、滑块手柄（.ui-slider）、:focus-visible 焦点环、prefers-reduced-motion 降级。

### Fixed

- 问答思考过程重写为显式可折叠区块：头部可点击（字数提示 + 展开/收起 chevron），不再依赖不起眼的文字链；展开后 320px 内滚动，流式"思考中"视图保持最近几行自动跟随。
- 修复分区切换卡顿：全岛入场动画移除 filter: blur（透明窗口大树重绘掉帧根因），Tab 内容转场改 opacity-only 快速交叉淡化并去掉 AnimatePresence mode="wait"（新分区不再干等旧分区退出），列表 stagger 降频。
- **多显示器定位全面修复**：新增 display-added/removed/metrics-changed 监听，热插拔/改分辨率/改 DPI 后岛与挂件自动重定位；跨 DPI 屏 setBounds 增加 60ms 校验重试（修复 DIP 换算竞态导致的偏移）；启动时渲染层水合后权威同步一次多屏偏好（修复主进程默认 follow=true 与 UI 默认固定主屏不一致导致的跳屏）；桌面挂件/钉屏便签从锚主屏改为跟随岛所在显示器。

## [0.2.0] - 2026-07-11

### Added

- 新增快捷、复盘、仓库三个主分区，主工作台扩展到 11 个分区。
- 新增项目工作台数据模型，连接资讯、待办、快捷执行、运行记录和成果物。
- 新增本地 Claude Code/Codex CLI 问答引擎、实时步骤和继续会话。
- 新增本地知识库 RAG，支持文件夹、源码/文本、PDF、DOCX 和网页。
- 新增第二大脑、命令面板、闪念胶囊、截图工坊、屏幕分析、工程计算和学习中心。
- 新增每日复盘、周报、工作洞察、番茄钟、成长记录和自动化规则。
- 新增 GitHub 仓库浏览、搜索、README 摘要、收藏和本地 Git 仪表盘。
- 新增主题设计器、自定义主题、全屏工作台、桌面挂件、会议勿扰和歌词能力。

### Changed

- 快捷模块重构为工程工作流编排器，内置 12 条开发验收、Git、Agent、MATLAB/Simulink 和需求工作流。
- 待办升级为任务执行系统，增加看板、项目、依赖、验收、精力、工时、归档、批量处理、Markdown 导入导出和完整 AI 工具集。
- 灵感便签增加双链、关系图、模板、快照、放映、回收站、批量管理、桌面便签和 27 项管理工具。
- Markdown 工作台增加本地打开/保存、分栏/阅读、目录、查找替换、快照、Zen、PDF/HTML/文本导出和 AI 写作工具。
- 资讯升级为情报工作台，增加观察清单、信号、雷达、多源综合、关联推荐、项目归属、转待办和成果沉淀。
- 终端增加多会话管理、命令中心、历史/收藏、目录跟踪、输出搜索和大屏模式。
- 问答增加知识库模式、剪贴板图片/聚类、引用追问、气泡内追问和多模型切换。
- Electron 升级到 39，React 保持 19，Vite 保持 5.x。

### Fixed

- 统一网页、文件、文件夹、会议、本地 Markdown 和原生对话框的外部让位流程，避免透明置顶窗口覆盖目标应用。
- 修复 Agent 交互提醒链路、桥发现文件被测试覆盖、窗口宽度调整、中文 PowerShell 输出、SVG 主题色和卡片入场动画等问题。
- 强化设置原子写、坏配置备份、主题兜底和 StrictMode 单次水合。

### Security

- 快捷工作流新增危险命令分类和不可绕过的确认关口。
- 知识库、GitHub、LLM 与外部网络请求统一收口到主进程/preload 契约。
- 外部打开、原生对话框和最高层窗口控制统一进入 `ExternalYieldController`。

## [0.1.0] - 2026-07-03

- 首个公开版本：Agent 监控与审批、计划审阅、问答、待办、灵感便签、资讯、终端、设置和顶部迷你条。
