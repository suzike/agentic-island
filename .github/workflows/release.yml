name: Build and publish Windows release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing draft release tag
        required: true
        default: v0.6.8

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Run offline tests
        run: npm test

      - name: Build NSIS installer
        run: npm run package

      - name: Generate checksum
        shell: pwsh
        run: |
          $installer = Get-ChildItem dist/Agentic-Island-Setup-*.exe | Select-Object -First 1
          $hash = (Get-FileHash -Algorithm SHA256 $installer.FullName).Hash.ToLowerInvariant()
          "$hash  $($installer.Name)" | Set-Content dist/SHA256SUMS.txt -Encoding ascii

      - name: Upload assets and publish draft
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          $installer = Get-ChildItem dist/Agentic-Island-Setup-*.exe | Select-Object -First 1
          # latest.yml + blockmap 是 electron-updater 发现与差量下载更新的必备资产，缺一不可
          $updateMeta = @($installer.FullName) + @(Get-ChildItem dist -File | Where-Object { $_.Name -in @('latest.yml', 'SHA256SUMS.txt') -or $_.Name.EndsWith('.blockmap') } | ForEach-Object { $_.FullName })
          gh release upload $env:RELEASE_TAG @updateMeta --clobber
          gh release edit $env:RELEASE_TAG --draft=false --latest
